
import UserPanel from "@/components/dashboard/UserPanel";

export default function EducationalMaterialPage() {
  return (
    <UserPanel
      title="Configuración"
      description="Elegí las comunicaciones que querés recibir por este canal."
      imageUrl="/dashboard/config.png"
      items={[]}
    />
  );
}